<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.geometry.Insets?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="app.controllers.AddDeviceController">

    <top>
        <Label text="إضافة / تعديل جهاز"
               style="-fx-font-size: 20px; -fx-font-weight: bold; -fx-padding: 10 0 10 0;"
               BorderPane.alignment="CENTER"/>
    </top>

    <center>
        <VBox spacing="10">
            <padding>
                <Insets top="15" right="15" bottom="15" left="15"/>
            </padding>

            <HBox spacing="10">
                <Label text="اسم الجهاز:"/>
                <TextField fx:id="deviceNameField" prefWidth="300"/>
            </HBox>

            <HBox spacing="10">
                <Label text="الصنف:"/>
                <ComboBox fx:id="itemComboBox" prefWidth="250"/>
                <Label text="الكمية:"/>
                <TextField fx:id="quantityField" prefWidth="100"/>
                <Button fx:id="addItemButton" text="إضافة"
                        style="-fx-background-color:#16a34a; -fx-text-fill:white;"/>
            </HBox>

            <TableView fx:id="componentTable" prefHeight="350">
                <columns>
                    <TableColumn fx:id="colItemName" text="اسم الصنف" prefWidth="400"/>
                    <TableColumn fx:id="colQuantity" text="الكمية" prefWidth="150"/>
                </columns>
            </TableView>
        </VBox>
    </center>

    <bottom>
        <HBox alignment="CENTER" spacing="10">
            <padding>
                <Insets top="15" right="15" bottom="15" left="15"/>
            </padding>

            <Button fx:id="saveDeviceButton" text="💾 حفظ الجهاز"
                    style="-fx-background-color:#3b82f6; -fx-text-fill:white; -fx-font-size:14; -fx-padding:6 20 6 20;"/>
        </HBox>
    </bottom>

</BorderPane>
